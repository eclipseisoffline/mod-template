{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    {
      "name": "eclipseisoffline",
      "contact": {
        "homepage": "https://github.com/eclipseisoffline"
      }
    }
  ],
  "contact": {
    "homepage": "https://github.com/${github_repository}",
    "issues": "https://github.com/${github_repository}/issues",
    "sources": "https://github.com/${github_repository}"
  },
  "license": "CC0",
  "environment": "*",
  "entrypoints": {
    "main": [
      "xyz.eclipseisoffline.modtemplate.fabric.ModTemplateFabric"
    ]
  },
  "mixins": [
    "mod-template.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "minecraft": "${minecraft_version}"
  }
}
